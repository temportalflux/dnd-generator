{
	"rows": [
		{
			"source": "{roll:race/$(description.race.value)/name-$(identity.genderIdentity)}"
		},
		{
			"source": "{roll:race/$(description.race.value)/name-$(identity.genderIdentity)}"
		}
	]
}